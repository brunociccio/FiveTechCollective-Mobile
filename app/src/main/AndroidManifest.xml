<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <activity
            android:name=".view.PaginaCinco"
            android:label="@string/activity_label_pagina_cinco"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".view.PaginaQuatro"
            android:label="@string/activity_label_pagina_quatro"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".view.PaginaTres"
            android:label="@string/activity_label_pagina_tres"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".view.PaginaDois"
            android:label="@string/activity_label_pagina_dois"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
